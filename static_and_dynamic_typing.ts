var person;
person = "Husnain Maqsood";
let substring = person.substr(1,8);
console.log(substring);

person = 1;

//substring =  person.substr(2,5); // this line will give us an error because now person has become of number type and numbers don't have substring function
